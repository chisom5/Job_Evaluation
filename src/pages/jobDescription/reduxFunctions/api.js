// import { makeGetRequest, makePostRequest } from "../../../helpers/httpHelpers";
// import baseUrl from "../../../config/baseUrl";

// export const fetchAppointments = (query) =>
//   makeGetRequest(`${baseUrl}/appointments`, query);

// export const fetchAppointment = (id) =>
//   makeGetRequest(`${baseUrl}/appointments/${id}`);

// export const postAppointment = (body) =>
//   makePostRequest(`${baseUrl}/appointments`, body);

// export const postAppointmentAction = (id, actionType) =>
//   makePostRequest(
//     `${baseUrl}/appointments/${id}/action?actionType=${actionType}`
//   );
